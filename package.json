{
  "name": "onepark",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@opac/eslint-config": "workspace:*",
    "@opac/tailwind-config": "workspace:*",
    "@opac/typescript-config": "workspace:*",
    "better-commits": "^1.15.7",
    "prettier": "^3.3.3",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv-cli": "^7.4.2"
  }
}
